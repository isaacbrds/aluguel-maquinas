<div class="container">
  <div class="row mt-4">
    <div class="d-flex">
      <div class="flex-grow-1">
        <h3 class="text-center font-weight-light my-4">Lista de Clientes</h3>
      </div>

      <div class="list-actions">
        <% if policy(Customer).create? %>
          <%= link_to 'Novo', new_customer_path, class: "btn btn-primary" %>
        <% end %>
      </div>

    </div>

    <%= render @customers %>
  </div>
</div>